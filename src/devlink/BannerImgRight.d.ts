import * as React from "react";
import * as Types from "./types";

declare function BannerImgRight(props: {
  as?: React.ElementType;
  headline?: React.ReactNode;
  title?: React.ReactNode;
  subTitle?: React.ReactNode;
}): React.JSX.Element;
