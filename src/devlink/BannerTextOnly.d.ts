import * as React from "react";
import * as Types from "./types";

declare function BannerTextOnly(props: {
  as?: React.ElementType;
  title?: React.ReactNode;
  headline?: React.ReactNode;
  text?: React.ReactNode;
}): React.JSX.Element;
