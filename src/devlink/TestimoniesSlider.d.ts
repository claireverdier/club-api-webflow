import * as React from "react";
import * as Types from "./types";

declare function TestimoniesSlider(props: {
  as?: React.ElementType;
  sliderId?: Types.Basic.IdTextInput;
  headline?: React.ReactNode;
  title?: React.ReactNode;
}): React.JSX.Element;
